import 'package:flutter/material.dart';
import 'main.dart';

class WorkoutDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
   return Scaffold(
      appBar: AppBar(
        title: Text("Workout Detail")
      ),
      body: Container(
        child: Text("WorkoutDetail"),
      ),);
  }
}